timeout: 10m
format: table
dependency-tree: true
list-all-pkgs: true
exit-code: 1
image:
  skip-dirs:
    - examples/*
severity:
  - HIGH
  - CRITICAL
scan:

config:
  misconfig-scanners: Terraform

ignorefile: .trivyignore
